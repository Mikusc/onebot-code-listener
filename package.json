{
  "name": "onebot-code-listener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NapCat / OneBot v11 group message code extraction chain validation tool.",
  "scripts": {
    "start": "node src/index.js",
    "setup": "node scripts/setup.js",
    "dashboard": "node scripts/dashboard.js",
    "doctor": "node scripts/doctor.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/extractor.js && node --check src/store.js && node --check src/queue.js && node --check scripts/setup.js && node --check scripts/doctor.js && node --check scripts/dashboard.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "clipboardy": "^4.0.0",
    "ws": "^8.18.0"
  }
}
